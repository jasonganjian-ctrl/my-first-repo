# my-first-repo

A simple calculator utility in Python.

## Usage

```python
from calculator import add, subtract, multiply, divide

add(2, 3)       # 5
subtract(10, 4) # 6
multiply(3, 4)  # 12
divide(10, 2)   # 5.0
```
